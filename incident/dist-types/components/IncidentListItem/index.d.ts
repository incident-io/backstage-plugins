import React from 'react';
import { definitions } from '../../api/types';
export declare const IncidentListItem: ({ baseUrl, incident, }: {
    baseUrl: string;
    incident: definitions['IncidentV2ResponseBody'];
}) => React.JSX.Element;
